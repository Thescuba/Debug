Run the DEMO notebook to make sure all image are loaded. The program rename.py was added to make renaming img files simpler. Running rename.py in terminal will prompt the user for an extension, for examples if the extention was .PNG you would simply enter PNG to convert all files in the directory to .png.
